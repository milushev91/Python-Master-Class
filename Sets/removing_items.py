small_ints = set(range(21))
print(small_ints)

# clear() removes all items from a set
# small_ints.clear()
# print(small_ints) # set()

# to remove items they are two methods:
# 1. discard() - removes item if it finds it, if the item is not inside - nothing happens
# 2. remove() - removes item if inside, if it isn't raises an error
